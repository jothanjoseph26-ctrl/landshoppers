import { ProviderRoadmapStub } from "../_stub"

export default function ProviderJobsPage() {
  return (
    <ProviderRoadmapStub
      title="Jobs & project tracker"
      specId="PRV-04"
      summary="Kanban over accepted quotes once lead state machine exists."
    />
  )
}
