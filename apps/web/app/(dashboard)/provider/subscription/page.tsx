import { ProviderRoadmapStub } from "../_stub"

export default function ProviderSubscriptionPage() {
  return (
    <ProviderRoadmapStub
      title="Subscription & billing"
      specId="PRV-10"
      summary="Paystack plans + usage meters once provider billing slice lands (Stream 1/4)."
    />
  )
}
