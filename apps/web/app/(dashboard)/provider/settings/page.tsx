import { ProviderRoadmapStub } from "../_stub"

export default function ProviderSettingsPage() {
  return (
    <ProviderRoadmapStub
      title="Settings & integrations"
      specId="PRV-11"
      summary="Notifications, defaults, quote templates — mirror developer settings UX."
    />
  )
}
