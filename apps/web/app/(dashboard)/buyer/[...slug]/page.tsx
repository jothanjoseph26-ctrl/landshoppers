import { PortalPlaceholder } from "@/components/dashboard/portal-dashboard"

export default function BuyerSubRoutePage() {
  return (
    <PortalPlaceholder
      title="Buyer workflow"
      description="This buyer dashboard section is reserved for the next vertical slice. The route is available so users no longer hit a 404."
      primaryHref="/buyer"
      primaryLabel="Back to buyer dashboard"
    />
  )
}
