import { ListingDiscovery } from '@/components/listings/listing-discovery'

/** Redfin-class discovery shell: filters + URL-synced search (`/v1/search/listings`) + optional split map. */
export default function ListingsPage() {
  return <ListingDiscovery />
}
